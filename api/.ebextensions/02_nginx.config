# Increase nginx timeouts and body size for large search responses
files:
  "/etc/nginx/conf.d/proxy.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 1M;
      proxy_read_timeout   60s;
      proxy_send_timeout   60s;
